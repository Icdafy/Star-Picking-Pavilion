# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [0.0.15] - 2026-07-31

本版本取消安装包体积上限。发布门禁不再因为体积失败，功能迭代不必再和一个与技术现实无关的数字较劲；包边界、密钥与依赖检查一条未减。

### Removed

- 移除 ASAR 与安装包的体积上限（`MAX_ASAR_BYTES` / `MAX_INSTALLER_BYTES`）。`npm run verify:package` 不再有任何绝对体积门槛，也不再导出这两个常量。

### Changed

- `npm run verify:package` 改为报告 ASAR 与安装包的精确字节数（同时给出 MiB），供发布记录比对；只在产物缺失或不是文件时失败。
- RELEASING.md 用实测数据说明为什么不继续压体积：win-unpacked 共 320.4 MiB，Chromium 运行时独占 232.8 MiB（72.6%），本项目全部代码与内置字体合计 11.9 MiB（3.7%）。

### Fixed

- `build.compression` 保持不设置（默认 `normal`）。实测 `store` 为 100,336,559 字节、默认为 99,330,701、`maximum` 为 99,330,670——默认档已贴近压缩极限，`maximum` 只省 31 字节，不值得引入一个买不到收益的构建旋钮。

### Security

- 体积上限之外的包边界检查全部保留：根目录白名单与数据库/日志/临时文件/文档目录黑名单、仅允许 lockfile 中的生产依赖、私钥与 GitHub/AWS/模型 Key 扫描、`resources/` 逐项白名单、LICENSE 与第三方声明校验。误打 dev 依赖、漏排除文档目录、把用户数据库塞进包里这些真实的膨胀成因依旧会让发布失败，并直接指出是哪个文件。
- 新增测试确保体积上限不会以别的名字悄悄回来：`verify-package` 不得导出任何 `MAX/LIMIT/BUDGET/CEILING` 类常量，源码中不得再出现「超出上限」的失败路径。

### Compatibility

- 仅影响发布门禁与构建配置。应用行为、数据库结构、界面、用户数据与既有设置全部不变；v0.0.14 的八段式管线与单模型架构原样保留。

## [0.0.14] - 2026-07-31

本版本把情报处理补成完整的八段式管线（结构化 → 数据清洗 → 预筛 → 标注 → 实体提取 → 原子事件分离 → 聚类 → 语义合并），并把全部模型调用统一到 DeepSeek-V4-Flash-0731。新增的四段全部是纯代码，请求数不增加。

### Added

- 新增结构化与数据清洗层：入库前规范化 URL（剥离 utm/spm/share 等跟踪参数）、还原 HTML 实体、剪除站点后缀与信源前缀、折叠全角字母数字、截去「责任编辑 / 免责声明 / 扫码关注」类正文尾巴。
- 新增按规范化地址的入库去重：同一篇文章从不同入口带着不同跟踪参数进来，只会落库一条。
- 新增标注与实体提取：词库分组即主题；实体走「词库通道 + 模型通道」双路合并并做别名归一（「蓝箭」「LandSpace」归入「蓝箭航天」），按机构、型号、场站、地域、人物、政策六类落库。
- 新增原子事件分离：一条资讯里的多件事被拆成「主体 · 动作 · 客体」，动作先归入 14 个动作类再生成事件键，同一件事的不同措辞落到同一个键。
- 新增语义合并通道：主事件键相同、或锚点实体重叠且动作类一致的报道会被并簇，补上字面相似度并不到的同一事件。
- 新闻卡片新增实体标签（点击即按该实体检索）与原子事件区块（仅在确实拆出多件事时出现）。
- 实体名进入全文索引：检索「蓝箭航天」时，标题里只写了「蓝箭」的报道同样会被列出。

### Changed

- 全站只调用 `deepseek-v4-flash`（当前指向 DeepSeek-V4-Flash-0731，调用方式不变）。设置页的「预筛模型 / 评分模型」两个字段收敛为单一「分析模型」。
- 五维评分、摘要、研判、标签、实体与原子事件由同一次模型调用产出，请求数与升级前持平。
- 聚类的锚点只接受机构、场站与人物：技术路线词（液氧甲烷、运载火箭、卫星互联网等）仍作为标签展示，但不再参与并簇判定。
- 分析循环会顺带按清洗版本补洗历史数据，升级后无需手动全量重跑管线。

### Removed

- 移除 `deepseek-v4-pro` 及两段式模型配置。旧设置里残留的 `prefilterModel` / `scoringModel` 会在加载时收敛为单一 `model`，其中已退役的模型名一律回落到默认值；设置接口也不再接受写入这些名称。

### Fixed

- 入轨类动作补齐「送入/进入预定轨道」等常见通稿措辞，此前只能匹配「入轨」「发射」的写法。
- 正文尾巴的截断门槛由 40 字降到 12 字，「一句话正文 + 责任编辑」这类短通稿不再把模板尾巴带进摘要与索引。
- 别名归一后客体与主体同名时（词库把机型登记为厂商别名，如 AE200 → 沃飞长空）丢弃客体，事件键与卡片文案不再出现重复。

### Compatibility

- 数据库以增量迁移补充 `canonical_url`、`entities_json`、`topics_json`、`events_json`、`event_key` 与 `clean_version`，既有文章、星标、日报、设置、加密凭据、保留策略与界面偏好全部保留。
- 老数据这些列为空时，聚类自动退回纯字面通道，卡片不渲染实体与原子事件区块。
- v0.0.13 的每日研究归档、存储治理与安装包体积门禁继续有效。

### Tests

- 新增结构化、清洗、去重键、实体归一、锚点收紧、原子事件分离与事件键稳定性、语义合并双通道与跨领域禁并的单测。
- 更新设置相关单测与真实 Electron 流程，覆盖单模型字段、旧字段收敛与已退役模型名的写入拒绝。

## [0.0.13] - 2026-07-31

本版本把每日新闻沉淀为可持续积累、可校验、可用于后续自然语言分析的本地研究档案，并为低空经济和商业航天中的可信技术突破加入可解释的热度提升。

### Added

- 「设置 → 每日研究归档」新增默认关闭的自动保存开关、系统原生文件夹选择、立即保存、失败重试、下次运行时间、最近成功日期与待补齐状态。
- 每天本地时间 08:00 归档窗口 `(前一日 08:00, 当日 08:00]` 内按 `fetched_at` 归属的全部采集记录；`新闻简报.md` 用于阅读，`news.jsonl` 保留相关、无关和待分析记录，`manifest.json` 记录窗口、统计、版本与文件 SHA-256。
- 新增断点补齐：应用启动、系统唤醒或解锁时按日期顺序补存遗漏周期；完整档案幂等跳过，不完整或被修改的同名档案原样保留，并写入带时间戳的“补存”目录。
- 新增低空经济与商业航天技术突破分类器：只有允许分类中的明确技术对象、完成性动作以及 T1/T1.5 或多源印证同时满足时才加分；不确定措辞和营销噪声明确排除。
- 新闻卡片新增“技术突破”标识与证据说明，研究简报单独汇总可信突破。

### Changed

- 热度公式升级为 `min(100, 质量分 + 技术突破加成) × 0.5^(经过小时数 / (基础半衰期 + 突破延长))`；突破加成和半衰期延长均有限幅，不改变普通新闻的既有口径。
- 每日服务器任务固定在本地 08:00 生成日报；存储保留清理调整至 08:25，确保先形成当期研究档案。
- 数据库以增量迁移补充突破分数、加成、证据信号和评分版本，既有文章会继续兼容并可在后续分析中重算。

### Fixed

- 信源响应在 HTTP/1 背压期间关闭时，旧版运行时网络客户端可能从底层事件处理器抛出不可捕获断言并终止采集进程。采集与模型请求现显式使用包含上游修复的 Undici 8.9，并通过真实 103 个启用信源的严格审计。
- 技术突破的“多源印证”改按不同信源计数，同一家媒体的重复稿不再冒充交叉验证；完成动作必须是对象之外的独立语义，“可回收火箭”中的“回收”和“模拟”中的“拟”不会误触发规则。
- 自动补档在后台运行，不再阻塞主窗口；本地简报请求增加 30 秒超时和 64 MiB 流式上限，并按分钟校准系统时钟与时区变化。
- 启动或唤醒时通过持久化游标轮转复核一个历史归档，长期覆盖全部日期且不做阻塞式全量扫描；已经校验通过的“补存”目录会被复用，不再因原同名目录损坏而反复生成副本。

### Security

- 自动归档路径只能来自 Electron 原生目录选择器；渲染页面不能传入任意文件系统路径，后端令牌也不暴露给页面。
- 目标目录在写入前校验绝对路径、可写性、普通目录身份与重解析边界；所有文件先写入同级临时目录，清单最后生成，再原子提交。
- 归档只写入用户选择的本地目录，不由摘星阁自动上传；数据库保留清理和缓存治理不会删除该目录。

### Compatibility

- 每日研究归档升级后默认关闭；原有文章、星标、日报、备忘、设置、加密凭据、保留策略和界面偏好全部保留。
- v0.0.12 的存储治理、旧库保护和安装包体积门禁继续有效；卸载仍保留 `%APPDATA%\摘星阁` 用户数据及用户自行选择的外部归档目录。

### Tests

- 新增突破证据门槛、热度加成与衰减、固定 08:00 窗口、全量 JSONL、原子归档、哈希校验、幂等跳过、冲突补存、启动/唤醒补齐和并发互斥测试。
- 真实 Electron 流程覆盖原生选目录、开关持久化、手动保存、三个归档文件与哈希、重启状态恢复以及控制台/页面错误检查。

## [0.0.12] - 2026-07-30

本版本新增完整的本地存储治理：把数据库、可回收空间、桌面缓存、迁移残留和旧版数据分开展示，并以保守阈值自动控制长期采集产生的磁盘占用。

### Added

- 「设置 → 存储治理」新增受管总计、库内情报、待清理条目、数据库体积、可回收空页、桌面缓存、迁移残留和旧版数据分项；后端或桌面不可用时明确标注未知，不把未知值当成 0。
- 新增数据库安全压缩：`quick_check` 前后校验、WAL checkpoint、磁盘空间预检和手动操作入口。自动压缩仅在可回收空间至少 64 MiB、比例至少 25%、距上次压缩至少 30 天时运行。
- 新增桌面缓存白名单治理：缓存软上限 256 MiB，自动清理最多每 7 天一次；占用中的可再生成缓存安排在重启后处理。
- 新增迁移残留和旧版数据库审阅：只清理超过 7 天且精确匹配的临时残留；旧库迁移满 30 天、新库健康且路径重新核对后才开放原生确认框。

### Changed

- 每次保留清理后运行 `PRAGMA optimize`，数据库深度压缩与采集、分析、普通清理互斥。
- 桌面缓存上限通过 Chromium 参数约束；维护时间与待重启状态原子写入 `storage-maintenance.json`。
- 数据维护面板按实际内容宽度自动降栏，普通清理、数据库压缩、缓存清理和旧库确认分别呈现进度与错误。

### Security

- 缓存治理只允许 `Cache`、`Code Cache`、`GPUCache`、`DawnWebGPUCache` 和 `DawnGraphiteCache`；未知文件、未知目录、数据库、星标、备忘、设置和加密凭据不会被自动删除。
- 旧版数据删除只接受由物理路径、文件身份和精确侧车集合生成的短期候选 ID；目录联接、重解析点和开发数据边界会被物理路径复核。确认后如数据库或同名 `-wal`、`-shm` 发生替换、增减，操作立即中止；删除前先暂存完整确认集合，主文件失败时整体回滚，不接受渲染层传入路径，也不递归删除目录。

### Tests

- 新增数据库压缩阈值、空间不足、完整性校验、WAL 忙碌、调度互斥、缓存白名单、迁移残留、旧库宽限期、Windows 目录联接、确认期间文件替换、失败回滚、IPC 参数校验和存储面板状态测试。
- 真实 Electron 布局矩阵覆盖 4 种窗口尺寸、4 档缩放和 5 个核心视图，最小 800×600 下无横向溢出。

## [0.0.11] - 2026-07-28

本版本在保留全部产品功能、内置思源黑体和用户数据兼容性的前提下完成安全瘦身，并纳入 v4 界面精修。

### Changed

- 卡片按低空经济、商业航天使用领域左缘色条，移除元信息中的重复领域标签；评分胶囊收敛为辅助信息，星标与复制操作保持常态可见。
- 导航、领域筛选、缩放档位、数据条、深浅主题卡片与浮层重新梳理层级；氛围层降噪，现有交互和状态逻辑保持不变。
- 远程缩略图使用异步解码，日报间距从内联样式迁移到样式表。
- 长信息流卡片与日报区块使用 `content-visibility` 和固有占位，减少视口外内容的布局与绘制压力。
- electron-builder 排除生产依赖中的文档、示例与测试材料；ASAR 和安装包体积门禁收紧到不得超过 v0.0.10 实测产物。

### Removed

- 清理本地旧版安装包、历史解包目录、测试截图和临时 UX 截图脚本。用户数据库、设置、凭据、开发依赖和正式功能均未删除。

### Tests

- 新增 v4 卡片领域属性、异步图片解码、日报样式类、离屏渲染规则、依赖材料排除和体积预算回归测试。
- 完整验证继续覆盖单元/集成测试、真实 Electron 流程、生产依赖审计、信源严格审计、包边界、版本一致性、装机版同源启动与 Release 校验。

## [0.0.10] - 2026-07-25

只改发布前的信源自检门槛，应用行为、数据库结构与界面均未改动。

### Fixed

- 后端启动失败时应用会卡死而不是给出提示。兜底页此前用 `data:text/html` URL，而 Chromium 自 M60 起禁止顶层导航到 `data:`，这个兜底只会得到 `ERR_FAILED`；异常随后从 `createWindow` 逃逸，被外层误判为「数据迁移失败」，进而调用**模态**的 `dialog.showErrorBox`——在没有交互桌面会话的环境里它会永远挡着，进程再也退不出去。现改为：兜底页走 `file://` 加载随包的 `renderer/startup-failure.html`（不依赖那个刚刚失败的后端 origin），兜底自身的失败就地吞掉不外溢，模态错误框只在装机版弹出、开发与 CI 留日志。这条链在 CI 上连续烧掉三个 job 的全部时间预算。
- 测试助手 `startServer` 的清理钩子原本注册在「等待后端就绪」之后，于是就绪超时这条失败路径上根本不会注册清理：后端子进程成为孤儿，它握着 IPC 通道与 stdio 管道，测试运行器因此永远等不到自己退出。结果是「一条用例超时失败」被放大成「整轮静默挂死」，CI 上表现为跑到一半再无输出、直到 job 撞上 30 分钟上限被砍（连续发生两次）。清理改为在等待之前注册，就绪超时同时从 10 秒放宽到 30 秒——后端启动要开库、跑迁移、同步一百多个种子信源，双核 runner 叠加 4 路测试并发时 10 秒会被正常启动摸到。
- 同一个清理钩子按 `child.exitCode` 判断进程是否还活着，而被信号杀掉的进程 `exitCode` 恒为 `null`（信号记在 `signalCode`），于是它会去 `await` 一个已经发生过的 `exit` 事件并永远等下去。改用退出标志位，清理因此可重复执行且无竞态。
- `npm run audit:sources -- --strict` 此前只在「请求失败」时返回非零退出码，抓到 0 条的信源会被放行。可信源失效往往不是报错而是悄悄变空——工信部「司局动态」改成动态加载空壳就是这种形态，页面照常返回 200、正文列表却是空的。现在严格模式把空结果与请求失败一并判为不合格，`hasStrictAuditFailure` 单独导出并有单元测试覆盖。

## [0.0.9] - 2026-07-25

### Changed

- 桌面窗口最小尺寸调整为 800×600；顶栏、导航、筛选器和主要内容网格改为按实际内容宽度自适应重排。
- 八个主导航始终完整可见；热点、设置、信源、常用网址与日报在空间不足时自动降栏，不再依赖固定像素断点，也不再隐藏热点。
- 常用网址、信源和设置使用不会撑破容器的内在尺寸网格，表单、长文本与弹窗同步获得安全收缩和滚动边界。

### Fixed

- 修复“大 / 特大”缩放下组件已经放大、固定像素断点却未提前触发，造成顶栏过高、正文首屏受压和局部控件拥挤的问题。
- 工信部“司局动态”旧地址现只返回动态加载页面骨架，已迁移到可静态解析的新版新闻发布页；迁移保留老用户的信源 ID、历史文章、采集统计与启停状态。
- 限制 Node 单元测试并发为 4，并让真实 Electron 套件串行执行，避免 Windows 上大量服务进程同时启动，或干净安装首次下载 Electron 时发生目录创建竞态。

### Tests

- 新增 800×600、1080×680、1440×920、1920×1080 四种真实 Electron 窗口 × 四档缩放 × 五个核心视图矩阵，校验无横向溢出、八个导航完整可见且交互元素不越界。
- 完成 347 项单元/集成测试、2 项真实 Electron 流程，并实时审计全部 103 个已启用信源；严格模式会同时拦截空结果与请求失败，本次复查结果为 103 正常、0 空结果、0 失败。

## [0.0.8] - 2026-07-25

本版本只做排版。字体全面换成「中文思源黑体 + 英文 Times New Roman」，整套字号上抬约一档半，并把字号、间距、圆角、栏宽统一改为从同一个根字号派生的 rem，使界面在任意窗口尺寸与任意缩放档位下保持同一套比例关系。

起因是试用者反馈「字偏小，看久了眼睛累」。查下来不只是字号问题：v0.0.7 的字号阶梯最小到 11px，正文基准 14px；而间距、圆角、栏宽是写死的像素值，字号一旦调大，留白与圆角不跟着走，版面会散。所以这一版把标尺本身重做了。

### Added

- 随安装包内置思源黑体（Source Han Sans SC，Google Fonts 发行名 Noto Sans SC，可变字重 100–900，SIL OFL 1.1）。思源黑体不是 Windows 预装字体，只在 CSS 里写字体名的话，没装过的机器会静默回退到微软雅黑；内置之后所有人看到的是同一套字形。字体按 unicode-range 切成 101 个分片，一屏中文通常只触发 3–8 片（约 1 MB），而不是一次性读 4.6 MB。
- 界面缩放档位：小 / 标准 / 大 / 特大，在「设置 → 显示与排版」中切换，或用 <kbd>Ctrl</kbd>+<kbd>+</kbd> / <kbd>Ctrl</kbd>+<kbd>-</kbd> 增减、<kbd>Ctrl</kbd>+<kbd>0</kbd> 回到标准档。档位与主题一样在样式表生效前就写到 `<html>` 上，开屏不会先按标准档画一帧再跳。档位随其余界面偏好一起持久化。

### Changed

- 中文一律思源黑体，英文与数字一律 Times New Roman。实现上是一条字体栈：Times New Roman 在首位但没有汉字字形，汉字自动落到后面的思源黑体上。原先的三层字体（展示字得意黑 / 界面字系统黑体 / 阅读字仿宋）不再使用，层次改由字号、字重与字距拉开。Times New Roman 的数字本身就是等宽的，评分、排名与时间列的对齐不受影响。
- 字号整体上抬：正文基准 14px → 16px，卡片标题 17px → 20px，摘要与情报研判 15px → 18px，最小一档 11px → 12px。中文行距同步放宽（正文 1.65 → 1.75，长文 1.8 → 1.9），字距由像素改为 em，放大时字距跟着一起走。
- 根字号成为整套排版唯一的标尺：随窗口宽度在 15–18.5px 之间连续变化，再乘以用户选的缩放档位。字号、间距、圆角、栏宽全部改用 rem 从它派生，因此整体放大或缩小时是等比例缩放，不会出现「字变大了但盒子没跟上」。
- 导航条不再用写死的 1399px 断点让出边距，改为 `max-width: min(--shell, 100% - 2×gutter)`——栏宽本身会随根字号变化，写死断点像素值对不上。
- 打包体积上限相应抬高：ASAR 12 MiB → 18 MiB，安装包 110 MiB → 120 MiB。这两个数的作用是拦住数据库、日志、开发依赖被误打进包，不是压体积。

### Fixed

- 退出时后端可能被强杀而不是优雅关闭。`server.close()` 只等既有连接自己结束，而 `closeIdleConnections()` 只能收掉调用那一刻就空闲的连接；渲染层与本地服务之间全是 keep-alive 长连接，字体分片又会随新字符出现而随时发起请求，退出瞬间只要有一条连接不空闲，关闭就会一直悬着，最后由 Electron 那侧 5 秒的强杀兜底收场。现在改为先收空闲连接、再给在途请求 500 ms 宽限期，到点无条件断开。CI 上的实测关闭耗时从超过 5000 ms 回到 200 ms 量级。
- 设置页的缩放档位按钮曾复用领域筛选的 `.pill` 类名，导致领域筛选的 `$$('.pill')` 会把档位按钮的选中态一并清掉，还给它们绑上了 `setDomain(undefined)`。档位按钮改用独立类名，领域筛选的选择器同时收窄到 `.domain-pills .pill`。

### Removed

- 得意黑（Smiley Sans）字体文件及其 `@font-face` 声明。

## [0.0.7] - 2026-07-25

本版本只做一件事：把「捕捉信息」这个核心能力重做一遍。信源从 56 个扩到 113 个，新增覆盖两大领域的核心词库并让它成为检索入口，打分从「五个维度分」扩展为「五维分 + 词库贴合 + 多源印证 − 噪声形态」，精选门槛改为按目标精选率自适应。

起点是一次实测：v0.0.6 的主力信源（东方财富关键词检索）一直用时间排序取回结果，而东财是分词 OR 匹配——「蓝箭航天」被拆成「航」「空」这类弱片段，返回的是当天全网最新新闻。抽样 96 个关键词，其中 81 个的相关率低于 25%，多数为 0%。也就是说主力信源长期在往库里灌噪声。

### Added

- **核心词库**（`config/lexicon.json`）：15 组、207 个词条、570 个匹配面，覆盖低空经济（总纲 / 飞行器与技术路线 / 整机主机厂 / 无人机 / 适航监管 / 基建运营 / 海外）与商业航天（总纲 / 运载火箭 / 火箭企业与型号 / 卫星与星座 / 卫星企业 / 发射场 / 资本产业 / 海外）。每条带权重与别名，权重口径写在文件里。
- **词库检索面板**（顶栏检索框旁，或 <kbd>Alt</kbd>+<kbd>K</kbd>）：按组罗列全部核心词，**每个词实时显示它在本地情报库中的命中条数**，点击即检索。支持领域筛选、「仅看有命中」与文本过滤。命中数与「全部动态」完全同口径（同一检索路径、同一相关性过滤、同一事件簇折叠），面板写几条，点下去就是几条。
- 打分新增三个模型看不到、代码却算得出的信号：**词库贴合度**（按命中词权重和加成）、**多源印证**（事件簇内还有几家在报）、**噪声形态惩罚**（股评软文、综合汇总、机关党务）。三者均为饱和曲线，堆词堆信源刷不满分。
- **精选率自适应校准**（`config/scoring.json` 的 `adaptiveFeatured`）：取最近 14 天相关条目的质量分分布，找目标精选率（默认 14%）对应的分位数，与静态阈值的差作为全局偏移，各分类之间的相对高低保持不变。
- 聚类完成后新增一轮纯代码重算：多源印证要等簇成型才知道，不回头重算，「五家同时报道」这个最强信号就永远进不了质量分。
- 新增信源迁移机制（`sources.default.json` 的 `_migrations`）：支持改地址、原地修选择器、清退死源三种写法，在补新源之前执行，且全部幂等。改地址保留原有行的 id 与采集统计；清退只停用不删除。

### Changed

- **信源库 56 → 113 个**，全部在 2026-07-25 实测可达后才写入：
  - 官方一手新增载人航天工程办公室、国家发改委新闻发布、中国航天报、中国无人机产业联盟；
  - 央媒与科技媒体新增新华网科技、人民网科技、中新网滚动、新浪科技、钛媒体、爱范儿、Solidot（全量 feed，靠词库预筛过滤）；
  - 东财关键词线从 39 条扩到 84 条，覆盖词库中的核心主题与具名主体；
  - 新增微信公众号接入位（RSSHub / Wechat2RSS，默认停用，备注写明公共实例实测不可达、建议自建）。
- **东财适配器改为相关性优先**：主取相关性排序、再补一路时间排序保住时效，可翻多页（`eastmoney://关键词?pages=2`）。同一批关键词的相关率由 0–25% 升至 40–100%。
- **新增入库守卫**：标题与摘要都不沾关键词、也不落在同领域词库内的条目在入库前丢弃，噪声不再占用 AI 预筛的 token 与保留期。
- `keywords.js` 不再自带第二份词表，全部派生自词库——加新赛道只改一处。
- 预筛 prompt 补充「主旨不在本领域即判无关」，收紧顺带提及一次就被收进来的情况。
- 清退实测不可达的旧信源：国家航天局深链、航天科技集团 `n25/n144/n206`（404）、航天科工深链，均已迁到探测通过的地址；修正中国民航网的选择器（列表在表格里，旧选择器只抓到 1 条）。

### Fixed

- **修复事件簇串链**：overlap 系数的分母是较短者，14 字的标题只有 13 个 bigram，撞上 6 个就超过阈值；并查集又是单链接聚类，A~B、B~C 会把整条链并成一簇。实测 2000 条规模下出现过 149 条的巨簇。新增绝对交集下限与簇容量上限后，最大簇回到 12。
- **修复精选门槛被砍两遍**：启发式折扣与自适应偏移原本会叠加（70 × 0.85 − 8 = 51.5），导致 62.6% 的相关条目都被判为精选。两者现在互斥——分位数已经量过真实分布，不必再打折。修复后精选率回到 10.7%。
- 放宽词库的领域判定门槛：单个具名主体（中国商飞、山河智能）此前不足以定性，导致这些检索线的相关率为 0%。一条新闻的主语是这个赛道的公司，它就是这个赛道的新闻。
- 补齐词库覆盖缺口：北斗、航天科技各院、航天科工各院此前无法命中。
- 机关党务与礼仪性活动（党组理论学习、党课、表彰大会等）不再被当作行业情报——这类标题常带「民航局」「航天科技集团」等高权重词，此前会被判成强相关再乘上 T1 系数冲进精选。

### Tests

- 新增词库测试：覆盖度、匹配面无重复、长词优先、复读不刷分、领域判定、噪声三类形态、与 `keywords.js` 的派生关系。
- 新增打分测试：饱和曲线、三个信号各自的加成与封顶、分数夹取、旧调用签名兼容、偏移与折扣互斥、分类相对高低保持。
- 新增校准测试：分位数精确对齐目标精选率、样本不足不校准、夹取标记，以及「整批都不合格时宁可空着也不推垃圾」的质量地板。
- 新增采集器测试：地址解析与参数夹取、入库守卫的拦截与放行、种子库领域与规模约束、迁移清单三种写法的自洽性。
- 新增信源迁移的真库测试：保留 id 与采集统计、可反复执行、不复活已删源、目标地址被占用时不撞唯一约束、清退只停用不删除。
- 新增聚类测试：绝对交集下限切断短标题串链、簇容量上限生效且越界成员另成簇。

## [0.0.6] - 2026-07-25

此前五个版本都在打磨情报的「生产侧」——采集、评分、精选、长期驻留与界面。本版本补上一直缺失的「消费侧」：把看到的情报留得住，也传得出去。采集、AI 管线与凭据存储的行为未改变。

### Added

- 星标留存：任意情报卡片右下角可一键星标，新增「星标」视图按收藏时间倒序陈列，导航标签显示收藏总数。**星标情报永久豁免数据保留清理**，设置页的「待清理」计数同步排除它们。
- 星标不受相关性判定与事件簇折叠影响：被 AI 判为无关、或身处事件簇中却不是主条的报道，只要收藏了就一定留在收藏夹里。
- 情报导出：日报可整份复制为纯文本或导出为 Markdown 文件；信息流（精选 / 热点 / 全部动态 / 星标）可按当前领域、分类与检索条件整份带走，单次最多 200 条。
- 单条复制：每张卡片新增「复制」，一次取走标题与链接。
- 情报备忘：设置页的反馈框此前只写不读，提交后连自己都看不到。现在会列出最近 50 条，可随时回看与删除，且不受情报保留策略清理。
- 新增快捷键 <kbd>Alt</kbd>+<kbd>C</kbd> 复制当前视图（信息流复制列表，日报复制整份日报）。

### Changed

- 视图快捷键范围随「星标」标签从 `Alt+1`–`Alt+7` 扩展到 `Alt+1`–`Alt+8`。
- 卡片底部改为「说明性开关靠左、留存与分发动作靠右」的两段式布局；此前只有存在事件簇或五维分时才渲染底栏，现在每张卡片都有固定的操作入口。
- 导出与复制的排版在服务端完成，两种格式只有一份实现；界面只负责写入剪贴板或另存为文件。
- 界面偏好中的视图白名单接受 `starred`，重启后能回到收藏夹。

### Fixed

- 修复「待清理」计数与实际清理集合口径不一致的隐患：两者现在共用同一条 WHERE 条件。

### Security

- 导出内容只接受绝对 HTTP(S) 链接，`javascript:`、`file:` 与内嵌凭据的地址一律剔除。
- 另存为完全在本地完成（页面自建 `blob:` + `a[download]`），不经过任何外部服务，现有内容安全策略未放宽。
- 星标、导出与备忘接口沿用既有的回环地址、启动令牌与请求体上限校验。

### Tests

- 新增导出模块的格式、转义、链接协议、控制字符压平、空文档与文件名落盘回归。
- 新增星标切换、收藏排序、无关条目豁免、簇内非主条豁免、保留清理豁免、统计计数、导出筛选与备忘增删的 HTTP 级回归。

## [0.0.5] - 2026-07-25

本版本是一次纯界面版本：重写渲染层设计系统，修复若干长期存在的布局缺陷，并补齐键盘操作路径。数据、采集、AI、凭据与桌面生命周期的行为均未改变。

### Added

- 全键盘操作：`/` 或 `Ctrl+K` 聚焦检索，`Esc` 清空并退出输入，`Alt+1`–`Alt+7` 切换视图，`Alt+T` 换主题，`Alt+R` 立即采集，`Home` 回到顶部；设置页新增快捷键速查卡片。
- 卡片新增「五维研判」显式展开按钮。此前五维分解只能靠点击卡片空白处触发，没有任何可见入口，键盘也无法到达。
- 检索态上下文条：显示当前检索词与已列出条数，并提供一键清除；检索框内新增清除按钮与快捷键提示。
- 长列表新增「回到顶部」浮动按钮，滚动超过一屏后出现。
- 导航新增跟随当前标签滑动的激活指示块；顶部统计数字变化时做短促补间。

### Changed

- 建立设计令牌体系：字号、间距、圆角、动效曲线与语义颜色（accent / second / flag 各含 ink、soft、line 三档）全部令牌化，两套主题只覆写令牌，不再逐条重写规则。
- 字体分层。此前包括按钮、输入框、标签在内的全部界面文字都使用仿宋，小字号下笔画过细。现在控件与元信息改用系统黑体，摘要、情报研判、说明文字仍用仿宋，保留报馆气质的同时提升可读性。
- 卡片头部改为弹性布局，评分胶囊不再依赖标题与元信息上的固定右侧留白，长评分文本不会再压住标题。
- 宣纸白主题下调次要文字色，11–13px 标签在宣纸底上的对比度从 4.07:1 提升到 4.64:1，满足 WCAG AA。
- 全部可聚焦控件补齐统一的 `:focus-visible` 焦点环；标签页与视图建立 `tabpanel` 关联，展开控件补 `aria-expanded`。
- 「加载更多」与「重新生成」在请求期间进入禁用与忙碌态，避免重复提交。
- 页面滚动状态改为在滚动事件内直接判定，不再依赖 `requestAnimationFrame`；窗口隐藏到托盘时帧回调会被暂停，用它会漏更新。

### Fixed

- 修复导航条滚动一屏后跟随内容滑走的问题。`html, body` 的 `height: 100%` 把 `position: sticky` 的包含块钉死在视口高度，导致 sticky 只在首屏生效。
- 修复日期分组标题作为 sticky 元素没有背景、与下方滚动卡片叠字的问题，并让其与右侧热度栏一起按导航条实际高度定位（导航换行时自动跟随）。
- 修复时间轴时间点与竖轴线错位约 6.5px 的问题，两者改为共用同一几何基准。
- 修复实时高亮动画结束后不显式回到常态边框的问题。

## [0.0.4] - 2026-07-24

本版本为长期驻留使用补齐显式的桌面生命周期控制。两个新开关默认关闭，升级后不会自行改变现有启动或退出习惯。

### Added

- 设置页新增「桌面运行」卡片，可独立控制“关闭主窗口后在后台运行”和“登录 Windows 时自动启动”。
- 开启后台运行后，关闭主窗口会隐藏到系统托盘；托盘支持重新打开主窗口和彻底退出摘星阁，首次隐藏时会显示一次系统提示。
- 正式安装的 Windows 版本可注册登录启动；若同时开启后台运行，登录后使用 `--hidden` 静默启动，否则正常显示主窗口。

### Changed

- 第二实例会统一通过桌面后台控制器恢复、显示并聚焦既有窗口。
- `closeToTray` 作为严格布尔值写入版本化 `ui-preferences.json`；登录启动状态始终从 Windows 回读，不重复写入 JSON。
- 安装包白名单新增独立托盘图标资源，并由包审计验证。

### Fixed

- 托盘创建失败时不再进入无恢复入口的隐藏状态，而是安全降级为可见窗口并返回固定提示。
- 桌面设置更新改为串行提交，只渲染主进程确认后的状态；保存失败会回读实际状态，避免开关显示与系统状态不一致。

### Security

- 本版本未改变凭据、网络、AI、数据保留或信源策略。桌面设置仍通过白名单 IPC 暴露，渲染进程无法访问通用 Electron 能力。

### Tests

- 新增后台控制器、托盘生命周期、登录启动回读、IPC 与渲染控制器单元回归。
- 真实 Electron 测试覆盖关闭到托盘、第二实例唤醒、`--hidden` 冷启动、偏好持久化与优雅退出。

## [0.0.3] - 2026-07-24

本版本面向「装上就一直开着」的长期运行场景，解决库体积无限增长、后台周期性卡顿与失效信源空转的问题。

### Added

- 数据保留策略：情报按可配置天数（默认 180 天）保留，判为无关的噪声用更短的独立天数（默认 21 天）；到期条目连同 FTS5 全文索引行一起清除，事件簇随之收敛，清理后截断 WAL。每天日报后 20 分钟自动执行，设置页也可手动触发。
- 设置页新增「数据维护」卡片：显示库内情报数、本地库实际占用空间与待清理条数，并可直接调整两档保留天数。
- 信源失败退避：连续失败的信源按指数拉长重试间隔（一个采集周期起，上限 6 小时），成功即刻复位。信源页显示暂停状态与连续失败次数，并提供「立即重试」；手动「立即采集分析」始终忽略退避，全量重试。
- `GET /api/maintenance` 与 `POST /api/maintenance/prune` 两个本地接口，以及 `POST /api/sources/:id/retry`。

### Changed

- 事件聚类改用倒排索引求交集，替代逐对比较。在真实语料上结果与原逐对实现逐个相同，耗时下降约 5.6 倍。
- 聚类在分组未发生变化时不再写库。此前每 75 秒的分析循环都会重写窗口内全部 `cluster_id`，是 WAL 持续膨胀的主因。
- 热点榜的时间衰减热度改在 SQL 内计算并直接分页，不再每次请求把上千行读进内存后在 JS 里排序。
- `/api/stats` 的全表计数加 5 秒缓存，并在采集、信源变更后立即失效；界面每 18 秒的轮询不再反复全表扫描。
- 新增 `fetched_at`、`COALESCE(published_at, fetched_at)` 与 `(relevant, fetched_at)` 索引，覆盖统计、时间线排序与保留清理的查询路径。

### Fixed

- 修复信息流加载期间切换领域或分类被静默丢弃的问题：整表重载改为以最后一次请求为准，过期响应不再覆盖界面；分页追加仍保持串行。
- 修复检索词中的 `%` 与 `_` 被当作 LIKE 通配符的问题，短词降级检索现在按字面匹配。
- 右侧热度栏同样丢弃过期响应，快速切换领域不会再看到上一个领域的榜单。

### Tests

- 新增倒排索引与逐对比较在多阈值、多领域、含近重复语料下的逐对等价性回归。
- 新增保留清理的 FTS 同步、事件簇收敛与主条改选、日报独立保留期、单轮删除上限等回归。
- 新增失败退避的时长推导、到期判定、健康度分级，以及采集层「失败→跳过→到期重试→成功复位」与强制重试的集成回归。
- 新增热度排序、热度窗口回退、LIKE 转义、数据维护接口与保留天数校验的 HTTP 级回归。

## [0.0.2] - 2026-07-23

### Added

- 使用独立于随机本地端口的 `ui-preferences.json` 记住全部有意义的界面选择：主题、最后视图、领域、动态分类、日报日期、常用网址分类与星标，以及实时更新开关。
- 通过版本化白名单、原子写入和损坏回退，使上述选择在完全退出并重新打开桌面应用后仍能恢复。

### Fixed

- 修复 Electron utility process 的 `MessageEvent.data` 凭据确认未被识别，导致 DeepSeek API Key 虽已加密写入却在界面中显示保存失败、运行时无法调用的问题。
- 将 AI 设置更新、加密凭据保存和普通设置落盘串行为完整事务，避免并发更新失败回滚覆盖较新的成功设置。

### Tests

- 新增两次真实 Electron 冷启动回归测试，强制第二次启动使用不同随机端口，并通过本地模拟 DeepSeek 服务验证加密 Key 恢复和模型调用。
- 新增界面偏好存储、IPC 边界、渲染器恢复、凭据确认及设置事务的单元回归测试。

## [0.0.1] - 2026-07-21

### Added

- Windows x64 桌面安装包、自动更新元数据与本地 SQLite 情报库。
- 低空经济、商业航天信息流、全文检索、聚类、日报与信源管理。
- 可选 AI 五维评分、系统加密凭据存储与“云幄 · 常用网址”视图。

### Security

- 随机回环端口、启动令牌、严格 Host/Origin 校验与请求体限制。
- Electron sandbox、权限默认拒绝、CSP、安全外链与渲染崩溃恢复。
- 原子设置写入、SQLite 完整性检查、旧数据一致性迁移与优雅退出。
- 白名单打包、ASAR 审计、生产依赖审计、SBOM 与 SHA-256 发布门禁。

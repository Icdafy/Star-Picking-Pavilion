# 摘星阁 v0.1.0.2

v0.1.0.2 紧急修复长信息流滚动时的标题栏重叠与日期分组视觉割裂，并为浅色、深色主题分别补充一整套流体背景配色。数据库、HTTP API、用户数据目录、情报处理、壁纸资产和既有偏好格式保持兼容，可直接覆盖升级。

## 标题栏与滚动安全区

- 宽屏塔台、侧栏和信息流日期标题统一从 Electron 隐藏标题栏安全区之后开始粘附；向下滚动时不再进入窗口拖拽区或与最小化、最大化、关闭按钮重叠。
- 粘顶偏移继续根据塔台实际高度和界面缩放动态计算，800×600 至 1920×1080、四档缩放均保持正确间距。

## 日期分组液态玻璃

- “今天 / 昨天”等日期标题由横贯内容区的近实色薄纱改为紧凑液态玻璃胶囊与渐隐轨道，流体背景可以连续透出。
- 胶囊使用主题玻璃渐变、背景模糊、饱和度、镜面高光、细描边和轻阴影；粘顶时仍能遮住滚动到其下方的文字，兼顾质感与可读性。

## 主题流体配色

- 浅色主题新增 6 组预设：晴空蓝、雨后青、晨曦紫、珊瑚霞、日光金、冰川蓝。
- 深色主题新增 6 组夜航预设：星海蓝、墨玉青、深空紫、熔星红、琥珀夜、极夜冰蓝；每组都使用适合暗色界面的独立明暗值，避免发灰或刺眼。
- 切换深浅主题时配色面板自动换组；点击预设会同步色相、明暗、滑杆、选中态和 DSH 1.1.0 着色器变量。
- 预设沿用现有 `aquaHue` 与 `aquaBrightness` 偏好，不新增迁移负担；原有滑杆继续保留，可在任意预设基础上精细调整。

## 兼容性

- 公开 tag、Release 标题、安装包名与 Windows 文件版本为 `0.1.0.2`。
- npm 和 electron-updater 内部版本为 SemVer `0.1.2`，使已安装的 v0.1.0.1 能正确发现本次更新；`latest.yml` 继续符合 electron-updater 规范。
- 数据库表结构、HTTP API、用户数据目录、信源、星标、日报、研究归档、加密 API Key、壁纸资产和既有 Aqua 偏好字段均未改变。

## 验证

- 单元与集成测试覆盖日期分组结构、主题预设完整性、最小偏好持久化和发布元数据。
- 真实 Electron 布局矩阵覆盖 800×600、1080×680、1440×920、1920×1080 与小/标准/大/特大四档缩放。
- 长列表滚动回归实测标题栏、塔台、日期胶囊的边界间距，并验证两套主题预设的数量、点击交互、滑杆和着色器状态。
- 发布包继续执行 ASAR 白名单、生产依赖、敏感文件、许可资源、SHA-256 与 CycloneDX SBOM 检查。

## 下载与校验

本版本尚未进行代码签名，因此 Windows SmartScreen 可能显示“Windows 已保护你的电脑”或“未知发布者”。请只从本项目 GitHub Release 下载：

- `Star-Picking-Pavilion-Setup-0.1.0.2.exe`
- `SHA256SUMS.txt`

下载后在 PowerShell 中执行：

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.0.2.exe
Get-Content .\SHA256SUMS.txt
```

确认两处 SHA-256 完全一致后再安装。校验值不一致时请停止安装，并重新从本项目 Release 页面下载。

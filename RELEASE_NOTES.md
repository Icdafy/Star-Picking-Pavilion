# 摘星阁 v0.1.2

v0.1.2 修复自动更新中“显示 0.1.3”“点击安装提示失败、退出重开却已成功”的两个关联问题，并让窗口最右侧滚动条与 Aqua 深浅主题融为一体。业务功能、数据库、用户数据和既有设置均保持兼容，可直接覆盖升级。

## 自动更新修复

- 更新器内部仍使用递增的 SemVer `0.1.4` 与旧安装版比较，界面则从 GitHub Release tag/title 读取公开版本，只向用户显示 `0.1.2`。内部比较号不再出现在更新胶囊或桌面桥中。
- 点击“重启安装”后，应用先停止本地后端、每日归档监听和更新定时器，再启动 NSIS 安装器，避免安装器与仍在退出中的 Electron/utilityProcess 争用文件。
- 安装请求由等待返回的 IPC 调用改为单向通知，窗口随安装流程销毁时不会再把正常退出误判成请求失败。
- 更新胶囊新增“正在重启安装”状态并锁定重复点击；若安装器确实无法启动，应用仍会保留明确错误提示。

## 滚动条视觉统一

- 页面和内嵌滚动区统一使用 8px 细滚动条、透明轨道和全圆角滑块，减少窗口右缘的突兀色带。
- 深空夜航与宣纸白主题分别使用低对比度滑块色；悬停时才提高主题强调色，兼顾界面连续性与可发现性。
- 滚动条颜色完全由现有主题令牌驱动，切换主题时与背景、玻璃描边和控件轨道同步更新。

## 兼容性

- 数据库结构、HTTP API、用户数据目录、凭据、壁纸、星标、日报和界面偏好格式均未改变。
- v0.1.1 安装版可自动发现 v0.1.2；此前下载的更新缓存不影响本版本重新检查和安装。

## 版本与验证

- 公开 tag、Release 标题、安装包名与 Windows 文件/产品版本为 `0.1.2`。
- npm 和 electron-updater 内部版本为 SemVer `0.1.4`，仅用于保证从内部版本 `0.1.3` 正常升级。
- 单元测试覆盖公开版本解析、后端关闭与 NSIS 启动顺序、重复安装保护、安装失败恢复和更新胶囊状态。
- 真实 Electron 测试与发布门禁继续覆盖窗口布局、ASAR 白名单、生产依赖、敏感文件、许可资源、SHA-256 与 CycloneDX SBOM。

## 下载与校验

本版本尚未进行代码签名，因此 Windows SmartScreen 可能显示“Windows 已保护你的电脑”或“未知发布者”。请只从本项目 GitHub Release 下载：

- `Star-Picking-Pavilion-Setup-0.1.2.exe`
- `SHA256SUMS.txt`

下载后在 PowerShell 中执行：

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.2.exe
Get-Content .\SHA256SUMS.txt
```

确认两处 SHA-256 完全一致后再安装。校验值不一致时请停止安装，并重新从本项目 Release 页面下载。

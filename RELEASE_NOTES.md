# 摘星阁 v0.0.6

前五个版本都在打磨情报的**生产侧**——把信源采回来、让 AI 把分打准、让应用能一直开着、把界面做扎实。这一版补上一直缺的**消费侧**：情报看完之后，要留得住，也要传得出去。

## 亮点

- **星标留存，而且真的留得住。** 任意卡片右下角一点即可收藏，新增「星标」视图按收藏时间倒序陈列，导航标签上带计数。关键在于：**星标情报永久豁免数据保留清理**。此前保留策略到期就删，收藏也拦不住；现在收起来就是收起来了，设置页的「待清理」计数也同步排除它们。
- **收藏夹里不会凭空少东西。** 被 AI 判为无关的条目、以及身处事件簇中却不是主条的报道，在信息流里本来就看不到或被折叠掉。只要你收藏了，它们就一定还在收藏夹里。
- **日报可以整份带走。** 「复制日报」得到一份纯文本，直接贴进微信群或邮件；「导出 .md」存成 Markdown 文件，贴进笔记或仓库。此前一份日报要转给同事，只能一条条手抄。
- **列表也能整份带走。** 精选、热点、全部动态与星标都可按当前领域、分类与检索条件导出，单次最多 200 条，导出时始终从第一条开始，与你翻到第几页无关。
- **单条复制。** 每张卡片新增「复制」，一次取走标题与链接。
- **情报备忘不再是黑洞。** 设置页的反馈框此前只写不读，提交完连自己都看不到。现在会列出最近 50 条，可回看可删除，也不受情报保留策略清理。
- **新快捷键** <kbd>Alt</kbd>+<kbd>C</kbd> 复制当前视图；视图快捷键随「星标」标签扩展为 <kbd>Alt</kbd>+<kbd>1</kbd>–<kbd>8</kbd>。

## 升级说明

数据库会在首次启动时幂等补上 `starred` 与 `starred_at` 两列，既有文章、信源、日报与设置一律不受影响，无需任何手工迁移，也可以随时降级回 v0.0.5（届时星标列会被忽略）。

主题、视图、领域、分类、日报日期、常用网址星标与实时开关照常从 `%APPDATA%\摘星阁\ui-preferences.json` 恢复。「桌面运行」的两个开关维持你升级前的状态。

采集、AI 两段式管线、五维评分、精选阈值与凭据存储的行为均未改变。

## 安全与隐私

导出内容只接受绝对 HTTP(S) 链接，`javascript:`、`file:` 与内嵌凭据的地址一律剔除；标题中的 Markdown 语法字符会被转义。另存为完全在本地完成——页面自建 `blob:` 配合 `a[download]`，不经过任何外部服务，现有内容安全策略未放宽，页面脚本仍一律外置且不使用内联事件处理器。

星标、导出与备忘三组接口沿用既有的回环地址绑定、启动令牌与请求体上限校验。API Key 仍使用 Windows 当前用户的 `safeStorage` 加密能力保存。本版本未改变凭据、网络、AI 或信源策略。

## 安装提示

本版本未签名。Windows SmartScreen 可能提示未知发布者；请先使用随 Release 提供的 `SHA256SUMS.txt` 核验安装包，再决定是否运行。

系统要求：Windows 10/11 x64。卸载默认保留 `%APPDATA%\摘星阁` 中的数据。

启用 AI 后，文章标题、摘要等待分析文本会发送给用户配置的模型服务。详情见 README 的隐私说明。

# 摘星阁 v0.0.9

这一版把 v0.0.8 的等比例缩放继续向下做实：不仅文字和间距会一起变，整个界面也会根据窗口与缩放后的实际可用宽度完整自适应重排。

从 800×600 起，窗口与四档缩放的每一种组合都采用完整自适应布局。

## 完整自适应排版

- 桌面窗口现在允许缩小到 800×600。
- 小 / 标准 / 大 / 特大四档缩放都会参与布局判断；字体越大，顶栏、网格和侧栏越早降栏，不再等固定像素断点。
- 精选、热点、全部动态、星标、情报日报、常用网址、信源、设置八个主导航始终完整可见，不使用横向滚动，也不折叠到隐藏菜单。
- 顶栏搜索与动作组、领域与分类筛选器、日报动作、设置表单都可受控换行。
- 信息流空间不足时，右侧“当前热点”会完整移动到主内容下方，而不是消失。
- 常用网址、信源与设置网格使用安全的内在尺寸；长文本、表单控件和弹窗不会撑破窗口。

真实 Electron 回归覆盖 800×600、1080×680、1440×920、1920×1080 四种窗口，叠加四档缩放和精选、日报、常用网址、信源、设置五个核心视图，共 80 个布局组合。每个组合都检查文档无横向溢出、八个导航完整可见、可交互元素不越界。

## 核心功能与信源复查

- 347 项单元与集成测试全部通过，覆盖采集、筛选、聚类、评分、设置、凭据加密、偏好持久化、星标、日报、备忘、导出、数据迁移与安全边界。
- 2 项真实 Electron 流程全部通过，覆盖安全启动、随机本地端口、设置保存、系统凭据加密、重启持久化、单实例、优雅退出以及完整布局矩阵。
- 2026-07-25 在隔离数据目录实时审计全部 103 个已启用信源：103 正常、0 空结果、0 失败。
- 审计发现工信部旧“司局动态”页面已改为动态加载空壳，直连无法获得正文列表；现已迁移到工信部新版新闻发布页。迁移会保留原信源 ID、历史文章、采集统计与用户启停状态。

信源状态属于发布时点的在线实测结果，外部站点之后仍可能出现短时网络波动或再次改版。

## 升级与数据

本次没有改变数据库结构、AI 评分、凭据加密或数据保留策略。既有文章、星标、日报、备忘、设置和界面偏好会原样保留；信源种子版本升级到 v6，只用于迁移已确认失效的工信部旧地址。

## 安装提示

本版本未签名。Windows SmartScreen 可能提示未知发布者；请从本项目 GitHub Release 下载 `Star-Picking-Pavilion-Setup-0.0.9.exe` 和 `SHA256SUMS.txt`，先核验 SHA-256，再决定是否运行安装包。

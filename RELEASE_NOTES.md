# 摘星阁 v0.1.6

- 新闻卡片分别展示报道发布时间与事件报道时差；将“迟报 N 天”改为“事后 N 天报道”，“时差未知”改为“事件日期待确认”，悬停说明正文未获取、时间不精确、证据缺失、日期歧义等具体原因。
- 原子事件区分已发生、计划、延期／暂停、失败／取消与状态待确认。明确发生状态不再被时间缺失掩盖，计划标签不表示已知具体日期。
- 日期支持连续相邻句证据与原文唯一日期补全，仍校验主体、动作、原文及日期一致性；不借用背景事件日期，不以发布日期代替发生日，修复未来月日误回退到上一年的问题。
- 正文抓取允许 HTTP→HTTPS 及跨站合规跳转，每跳先检查目标地址和目标站点 robots.txt；继续拒绝内网、禁止采集与访问验证。相对图片地址按最终网页地址解析；修复评论区验证码脚本误判，并识别东方财富正文区域。
- 有可用密钥时，每轮最多补提取 5 条历史事件，优先收藏、精选及最近 30 天收录新闻；失败间隔至少一小时、最多两次，记录结果且保留原评分、摘要与收藏。无密钥不发起补提取。
- 继续统一使用 `deepseek-v4-flash-vision-exp`，公众号采集仍遵循公开订阅与站点限制。

## 下载与校验

本版本尚未代码签名，Windows SmartScreen 可能提示未知发布者。请从本项目 Release 下载，核对 SHA256SUMS.txt 后安装。

安装包：`Star-Picking-Pavilion-Setup-0.1.6.exe`

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.6.exe
Get-Content .\SHA256SUMS.txt
```

附带 blockmap、latest.yml、SHA256SUMS.txt、CycloneDX SBOM 与第三方声明。

# 摘星阁 v0.1.3

v0.1.3 收敛自动更新的版本与安装语义，并升级窗口顶部和右侧滚动条的 Aqua 视觉。应用、安装器、Windows 文件属性、GitHub tag、Release 标题与安装包名统一使用公开版本 `0.1.3`。数据库、用户数据、凭据、壁纸、星标、日报和界面偏好均保持兼容。

## 版本统一与自动更新

- npm/Electron 应用版本、`buildVersion`、Windows 产品版本、安装器品牌和桌面桥统一为 `0.1.3`，安装过程不再出现 `0.1.4` 之类的内部产品版本。
- `v0.1.2` 曾被打包成内部 `0.1.4`。为保证该安装版仍能自动升级，`v0.1.3` 的 `latest.yml` 使用一次性传输桥 `0.1.5`；它只参与旧客户端的大小比较，不是产品版本，也不会进入安装器、Windows 属性或新版界面。
- 新客户端按 GitHub tag / Release 标题比较公开版本，因此安装完成后会忽略同一 `v0.1.3` 的传输桥，不会重复下载或循环安装。后续版本恢复公开号与更新元数据一一对应。

## 安装可靠性

- 点击“重启安装”后，先优雅停止本地后端、归档监听和更新定时器，再以静默 NSIS 更新模式替换文件并重新启动应用。
- 关闭 `autoInstallOnAppQuit`：下载完成后只有用户明确点击才会安装；若安装器真的无法启动，普通退出不会再把失败请求悄悄变成一次成功安装。
- 更新请求继续使用单向 IPC，并锁定重复点击；异步安装器错误会退出安装中状态并显示真实原因。

## 液态标题栏与滚动条

- 原生 Windows 按钮下方新增全宽液态流体玻璃层：实时折射页面 WebGL 流体，叠加 Aqua 青、靛双高光、镜面顶光与主题化边缘辉光。
- 标题栏保持可拖拽且原生最小化、最大化、关闭按钮不受遮挡；低动效、低性能和失焦状态会自动停止漂移动画。
- 页面最右侧与内嵌区域的滚动条改用 Aqua 主强调色，深色为星海青、浅色为雨后青；轨道、悬停、细圆角样式随主题同步切换。

## 版本与验证

- 公开版本：`v0.1.3`。
- 安装包：`Star-Picking-Pavilion-Setup-0.1.3.exe`。
- 发布门禁覆盖版本一致性、旧版更新桥、安装状态机、标题栏材质、滚动条主题、单元测试、真实 Electron 布局、生产依赖、ASAR 白名单、SHA-256 与 CycloneDX SBOM。

## 下载与校验

本版本尚未进行代码签名，因此 Windows SmartScreen 可能显示“Windows 已保护你的电脑”或“未知发布者”。请只从本项目 GitHub Release 下载：

- `Star-Picking-Pavilion-Setup-0.1.3.exe`
- `SHA256SUMS.txt`

下载后在 PowerShell 中执行：

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.3.exe
Get-Content .\SHA256SUMS.txt
```

确认两处 SHA-256 完全一致后再安装。校验值不一致时请停止安装，并重新从本项目 Release 页面下载。

# 摘星阁 v0.1.5

- 所有预筛、图文分析、原子事件提取、复杂研判和日报统一使用 `deepseek-v4-flash-vision-exp`，删除 Pro 路由与二次复核；旧配置自动迁移。
- 完全移除交叉验证模块、状态、证据面板和多源评分加成，关联报道仍可聚类浏览。
- 原子事件最多拆分 6 条，优先识别具体动作，保留型号、任务批次及融资轮次；新增股权变更类，避免把股东变更判成回收复用。计划、已发生和失败状态分别处理。
- 新闻右上角仅保留简短时差提示：原文明确事件日期后，直接与报道日期比较，无需等待其他信源。单篇新闻同样可显示“迟报 N 天”；无日期证据显示“时差未知”，计划显示“计划事件”。悬停查看事件日期、报道时间与计算口径。
- 时差按北京时间自然日计算，不以抓取时间代替报道时间；事件原文证据必须逐字匹配。无法从原文确定的真实日期保持未知。已知事件日期用于信息流排序和热度衰减。
- 修复同一图片渲染两次：仅显示新闻右侧的一张缩略图，优先使用视觉筛选的图片，悬停可看 AI 图片解读。删除下方大图区域。
- 已有原子事件在本地迁移；有可用密钥时，最近 30 天内最多 200 条旧分析渐进重判。新闻、星标、密钥和既有归档保留。公众号采集延续公开订阅方式。

## 下载与校验

本版本尚未代码签名，Windows SmartScreen 可能提示未知发布者。请从本项目 Release 下载，核对 SHA256SUMS.txt 后安装。

安装包：`Star-Picking-Pavilion-Setup-0.1.5.exe`

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.5.exe
Get-Content .\SHA256SUMS.txt
```

附带 blockmap、latest.yml、SHA256SUMS.txt、CycloneDX SBOM 与第三方声明。

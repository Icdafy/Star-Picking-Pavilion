# 摘星阁 v0.0.20

这一版扩充卫星专题词库与信源接入面，同时收敛信息流导航。新增信源按实际可用性决定默认启停，已知依赖登录、自建服务或当前不可达的入口不会在升级后自动请求；既有数据库、星标、日报、研究归档和用户设置保持兼容。

## 卫星专题词库（C 组）

- 新增“卫星制造技术”“卫星应用与通导遥”“发射与轨道”三个商业航天专题分组，覆盖星间链路、星上计算、通导遥一体化、卫星物联网、在轨服务、轨道转移等细分方向。
- 扩充 Iridium、AST SpaceMobile、Inmarsat、Maxar、Telesat、上海瀚讯、海格通信、欧比特等国内外卫星企业；词库现包含 18 个分组、268 个核心词和 739 个匹配面。
- 新增专题词继续遵守权重纪律：不把“卫星、发射、星座、轨道、载荷”等泛词提升为独立高权命中，避免放宽情报入库与领域判定门槛。
- AST SpaceMobile 与 Globalstar 作为不同运营商独立归一，避免实体、检索和评分串线。

## 信源集成（B 组）

- 信源种子库升级到 v7，新增 14 类、共 19 条接入位，总数达到 131 条；覆盖 36氪快讯、知乎/头条号 RSSHub、财经社区、研报平台、交易所公告、财联社电报和卫星主题公众号。
- API 采集器由单一东财检索扩展为 scheme 分派，新增 `cninfo://`、`sse://`、`szse://`、`cls://`，并对巨潮、上交所、深交所和财联社响应执行严格结构校验；异常响应会进入原有退避链路，不写入脏数据。
- 采集请求新增受限 POST、请求体和请求头覆盖能力，继续保留 HTTP(S) 地址校验、超时、重定向终点校验和 10 MiB 响应上限。
- 失效的 36氪官方 feed 通过幂等迁移转到 RSSHub 快讯路由，原有信源行、历史统计和用户启停状态保留。依赖登录 Cookie、自建 RSSHub、接口签名或当前不可达的入口均默认停用；发布前严格审计返回空列表/异常结构的同花顺、集思录和巨潮“卫星互联网”检索也保持停用。

## 信息流界面

- 移除独立“热点”导航、右侧 Hot Rail、`hot` 信息流查询和排行卡片模式，保留精选、全部动态、星标三类信息流；已保存的 `hot` 偏好自动回落到精选。
- 实时轮询不再刷新已移除的热度栏；无数据变化时直接重新排程。日报中的“当日热点”、热度衰减公式和技术突破加成继续保留。
- 主导航收敛为 7 个视图，并同步更新快捷键提示、响应式布局、导出与前端契约文档。

## 验证与兼容

- 本地完整回归覆盖 731 项单元/集成测试和 2 项真实 Electron E2E；浏览器复验确认 7 个主导航、新词库分组及信源列表正常渲染。
- 数据库表结构、用户数据目录、信源历史、星标、日报、研究归档与偏好文件格式不变，可直接覆盖升级。旧 `hot` 偏好会安全回落，不需要手工清理。

本版本未签名。Windows SmartScreen 可能提示未知发布者；请从本项目 GitHub Release 下载 `Star-Picking-Pavilion-Setup-0.0.20.exe` 和 `SHA256SUMS.txt`，先核验 SHA-256，再决定是否运行安装包。

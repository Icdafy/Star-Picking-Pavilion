# 摘星阁 v0.1.1

v0.1.1 修正“界面氛围”流体配色的色相映射：此前把目标色相直接当作 CSS 旋转量，导致星海蓝、墨玉青、深空紫、熔星红等名称、预览色块与实际背景错位。本版本统一目标色相、着色器旋转和旧偏好迁移，其他业务功能与数据格式保持兼容，可直接覆盖升级。

## 流体配色修复

- “星海蓝”稳定呈现蓝色，“墨玉青”呈现青色，“深空紫”呈现紫色，“熔星红”呈现红色；琥珀夜、极夜冰蓝及浅色主题六组预设也按同一色相模型校准。
- 设置面板继续显示直观的目标色相；渲染层会先扣除 DSH 基础流体的 220° 蓝色色相，再把正确旋转量交给 `hue-rotate()`。
- 预览色块、预设名称、滑杆数值、选中态和实际流体背景现在使用同一组目标色相。

## 旧偏好迁移

- 界面偏好格式升级为 v2。旧版已选择的命名预设会迁到对应的正确颜色，不需要手动重新选择。
- 旧版自定义色相会换算为等效目标色相，尽量保持升级前的视觉结果；主题、明暗、玻璃、壁纸、星鲸及其他界面偏好不变。
- 数据库、HTTP API、用户数据目录、情报处理、壁纸资产和业务设置格式均未改变。

## 版本与验证

- 公开 tag、Release 标题、安装包名与 Windows 文件版本为 `0.1.1`。
- npm 和 electron-updater 内部版本为 SemVer `0.1.3`，高于 v0.1.0.2 使用的 `0.1.2`，可正常发现本次更新。
- 单元与集成测试覆盖六组深浅主题预设、目标色相到旋转量的换算、v1 偏好迁移及发布元数据。
- 真实 Electron 测试验证预设点击后滑杆、选中态、明暗变量和实际旋转变量同步。
- 发布包继续执行 ASAR 白名单、生产依赖、敏感文件、许可资源、SHA-256 与 CycloneDX SBOM 检查。

## 下载与校验

本版本尚未进行代码签名，因此 Windows SmartScreen 可能显示“Windows 已保护你的电脑”或“未知发布者”。请只从本项目 GitHub Release 下载：

- `Star-Picking-Pavilion-Setup-0.1.1.exe`
- `SHA256SUMS.txt`

下载后在 PowerShell 中执行：

```powershell
Get-FileHash -Algorithm SHA256 .\Star-Picking-Pavilion-Setup-0.1.1.exe
Get-Content .\SHA256SUMS.txt
```

确认两处 SHA-256 完全一致后再安装。校验值不一致时请停止安装，并重新从本项目 Release 页面下载。
